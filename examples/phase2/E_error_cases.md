# 錯誤案例測試文件

此文件故意包含多種語法錯誤，用來驗收 word-engine 的 QC 系統是否能正確攔截。

### 錯誤一：標題跳級（H1 直接跳到 H3，跳過 H2）

#### 這是 H4，先前沒有 H2 和 H3

這會觸發「Heading jump」錯誤。

### 錯誤二：未閉合的程式碼區塊

```python
def unclosed():
    print("這個 code fence 沒有關閉")

### 錯誤三：空白標題

# 

正常段落繼續在這裡。

### 錯誤四：大量連續空白行（Linter 警告）




最後一段。
