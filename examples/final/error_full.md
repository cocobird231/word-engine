# 錯誤案例驗收文件

此文件故意包含多種錯誤。請執行 QC 後根據報告修正，直到 QC PASSED 為止。

以下錯誤類型均在本文件中，請根據 QC report 逐一修正：

### 第一個錯誤：標題跳級（H1 後直接接 H3，跳過 H2）

段落內容。

## 正常的 H2 節

以下為正常內容。

### 第三個錯誤：未閉合 inline code

請見這個 `未閉合的 inline code 範例。

### 第四個錯誤：未閉合粗體

這裡有 **未閉合粗體標記的文字。

### 第五個錯誤：空白標題

# 

正常段落在這裡。

### 第六個錯誤：過多連續空白行




這是空白行過多之後的段落（觸發 warning）。

### 第七個錯誤：未閉合的程式碼區塊（必須放最後）

```python
def broken():
    print("此 code fence 沒有結尾，此行之後所有內容被視為 code，其餘 lint 不會掃到")
